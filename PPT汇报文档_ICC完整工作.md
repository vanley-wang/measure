# ICC类器官药物评估框架 — 完整PPT汇报文档

---

## 一、论文背景回顾（PPT 第1页）

### 1.1 论文核心框架

```
原始形态学数据(11维) → KMeans聚类(4类) → 提取44维特征 → PCA(4PC) → F = 0.468P1+0.247P2+0.165P3+0.120P4
```

### 1.2 论文报告的关键数据

| 实验场景 | 模型类型 | 相关系数 r | 说明 |
|---------|---------|:---------:|------|
| 阿可拉定主实验 | 纯形态学模型 ΔF vs ATP | **0.906** | 基线模型 |
| 阿可拉定主实验 | **融合OAC模型** ΔF vs ATP | **0.938** | OAC引入后提升 |
| 联合用药泛化验证 | 融合OAC模型 ΔF vs ATP | **0.887** | 跨药物独立测试 |

**关键认知**：
- 论文输入PCA的是 **44维特征**（4个Cluster × 11个特征），不是15维
- PCA保留 **4个主成分**（Kaiser准则：特征值>1），累计方差贡献率 **70.82%**
- **KMO=0.742**（Middling，可接受），Bartlett p<0.001，数据适合PCA

### 1.3 我们的工作目标

> 在论文基础上，探索**两个方向**：
> 1. **增强方向**：通过添加Delta特征（动态变化）能否提升ICC上的性能？
> 2. **泛化方向**：该框架能否迁移到胃癌（GC）类器官数据？

---

## 二、实验路线总览（PPT 第2页）

```
                 ┌──────────────────────────────────┐
                 │       论文框架（基线）              │
                 │  44D → PCA(4PC) → F vs ATP        │
                 │  r=0.906(纯形态) / 0.938(OAC)     │
                 └──────────┬───────────────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          ▼                                    ▼
┌─────────────────────┐              ┌─────────────────────┐
│  方向1: ICC增强       │              │  方向2: GC泛化        │
│  +Delta/RelChange特征 │              │  跨癌种验证           │
│  45D → PCA → Score   │              │  GC重新训练全流程      │
│  r=0.931 (全量)       │              │  r=0.857 (全量)       │
│  ⚠️ KMO=0.446 过拟合  │              │  LOO CV=0.806 ✅      │
└─────────┬───────────┘              └─────────┬───────────┘
          │                                    │
          ▼                                    ▼
┌─────────────────────┐              ┌─────────────────────┐
│  PCA质量评估         │              │  框架迁移分析         │
│  KMO/Bartlett/VIF   │              │  PCA系数对比          │
│  特征子集基准测试     │              │  P2跨癌种稳定因子     │
│  最优: D5+Delta(24D) │              │  权重必须重新拟合      │
│  r=0.8735, KMO=0.853│              │                      │
└─────────────────────┘              └─────────────────────┘
```

**建议PPT配图**：用流程图展示两条主线及关键r值，用颜色区分ICC/GC。

---

## 三、方向1：ICC Delta特征增强（PPT 第3-5页）

### 3.1 改进动机

**问题发现**：论文的44维特征是**静态快照**（单日状态），缺少**动态变化信息**。

**改进思路**：通过添加Delta/RelChange特征捕获药物作用前后的变化。

```
原始特征: Healthy_Scatt_Mean_D5 (Day5绝对值)
新增特征: Δ Healthy_Scatt_Mean = D5 - D3 (变化量)
          RelChange_Healthy_Scatt_Mean = (D5-D3)/|D3| (相对变化)
```

**特征工程流程**：
```
原始11维形态学特征 + OAC
    ↓ KMeans聚类(4类) [使用论文预训练模型]
按Cluster分层统计中位数
    ↓ 计算
┌───────────────────────────────────────────────┐
│ Group 1: D5绝对值 (~14维)                      │
│ Group 2: Delta变化量 (~14维)    ← 新增         │
│ Group 3: RelChange相对变化 (~14维) ← 新增      │
│ Group 4: 聚类比例变化 (~6维)     ← 新增         │
└───────────────────────────────────────────────┘
    ↓ PCA降维 (输入~45维)
Score = Σ wᵢ·PCᵢ  →  vs ATP
```

### 3.2 核心结果

| 指标 | 论文融合OAC模型 | 我们的Delta方法 |
|------|:-------------:|:--------------:|
| Pearson r | **0.938** | **0.931** |
| p-value | <10⁻⁶ | **3.26×10⁻¹¹** |
| 输入维度 | 44D | 45D |
| PC1解释方差 | 33.16% | ~73.8% |

**建议PPT配图**：`fig2_score_atp.png` — 三合一图（PCA碎石图 + 权重柱状图 + Score vs ATP散点图, r=0.931）

**介绍话术**：
> "通过添加Delta/RelChange特征，将特征维度从44维扩展至45维，Score vs ATP的相关系数达到0.931，接近论文的0.938。p值更小（3.26e-11），统计显著性更强。PC1解释73.8%方差，信息高度集中。"

### 3.3 强相关Delta特征

| 特征 | 与Score的相关性 | 物理意义 |
|------|:---:|------|
| **Δ Healthy Scatt Mean** | **-0.960** | 药物处理后健康类器官光学衰减系数变化 |
| Red(H) Fraction D5 | 0.760 | 终点时大囊状健康态占比 |
| **Δ Healthy Fraction** | **0.613** | 健康类器官比例变化 |

**建议PPT配图**：四宫格散点图展示Delta特征与Score的关系

**介绍话术**：
> "Δ Healthy Scatt Mean一项就与综合得分达到r=-0.96的极强相关。药物有效时，健康类器官的光学衰减系数显著升高，Delta特征能精准捕捉这一动态变化。"

---

## 四、方向1的问题：PCA质量暴露（PPT 第6-8页）

### 4.1 问题发现：共线性严重

运行 `analyze_pca_quality.py` 后，发现严重问题：

**问题1：Delta vs RelChange 完全冗余**

| 特征对 | 相关系数 |
|--------|:-------:|
| `Delta_Healthy_ShortAxis` ↔ `RelChange_Healthy_ShortAxis` | **0.996** |
| `Delta_Healthy_Organoids_Surface` ↔ `RelChange_Healthy_Organoids_Surface` | **0.996** |
| `Delta_Healthy_Sphericity` ↔ `RelChange_Healthy_Sphericity` | **0.995** |
| `Δ Healthy Scatt Mean` ↔ `RelChange_Healthy_Scatt_Mean` | **0.993** |

**共发现 41 对 |r| > 0.9 的特征，65 对 |r| > 0.8！**

**建议PPT配图**：`feature_correlation_heatmap.png` — 相关性热力图

**介绍话术**：
> "相关性热力图清晰显示，Delta和RelChange两组特征之间存在完整的红色对角线（r>0.99），说明RelChange几乎不提供额外信息，与Delta完全冗余。"

**问题2：KMO检验不合格**

| 指标 | 论文（44维） | 我们（45维） |
|------|:----------:|:----------:|
| **KMO** | **0.742** ✅ | **0.446** ❌ |
| 条件数 κ | — | **40,993** ⚠️ |
| VIF>10 特征 | — | **35/35 (全部inf)** |
| Bartlett | p<0.001 ✅ | p<0.001 ✅ |

**介绍话术**：
> "KMO=0.446 < 0.5，属于Unacceptable级别。35个特征全部VIF>10（大部分为inf），条件数高达40,993。这意味着我们的45维特征矩阵存在严重的多重共线性，不适合直接做PCA。"

### 4.2 解决方案：特征子集筛选

**6个子集 × 4种PC策略的基准测试**：

| 子集 | 维度 | d/n | KMO | Kaiser PCs | Pearson r |
|------|:---:|:---:|:---:|:---:|:---:|
| A: D5 Only | 12 | **0.39** | **0.978** 🟢 | 2 | 0.8706 |
| B: Delta Only | 12 | 0.40 | 0.957 🟢 | 2 | 0.8101 |
| C: RelChange Only | 11 | 0.34 | 1.031 | 2 | 0.7901 |
| **D: D5 + Delta** ⭐ | **24** | **0.80** | **0.853** 🟢 | **4** | **0.8735** 🏆 |
| E: Delta + Fractions | 15 | 0.50 | 1.091 | 3 | 0.8136 |
| F: All Features | 35 | **1.09** 🔴 | **0.446** 🔴 | 6 | 0.8451 |

**建议PPT配图**：`feature_subset_benchmark.png` + `subset_pca_scree_grid.png`

**介绍话术**：
> "删除RelChange组后，D5+Delta（24维）是最优子集：KMO从0.446提升至0.853（Meritorious），d/n从1.09降至0.80（安全范围），4个Kaiser PCs，r=0.8735。值得注意的是，仅用12维D5绝对值（d/n=0.39）就能达到r=0.8706，说明论文的静态特征设计本身就很强。"

---

## 五、方向2：GC胃癌跨癌种验证（PPT 第9-11页）

### 5.1 实验设计

| 层面 | ICC（论文） | GC（本次验证） |
|------|------------|---------------|
| 癌症类型 | 肝内胆管癌 | **胃癌** |
| KMeans | ICC训练的 | **GC重新训练** |
| PCA系数 | ICC数据算出 | **GC重新训练** |
| F权重 | 0.468/0.247/0.165/0.120 | **GC重新拟合** |

> ⚠️ 关键认知：ICC和GC是两个完全独立的实验，KMeans、PCA、F权重均需重新训练。**不能直接比较r值高低**。

### 5.2 四步递进实验

| 步骤 | 方法 | r(F vs ATP) | 结论 |
|:---:|------|:---:|------|
| ① | ICC KMeans + ICC PCA + 论文权重 | -0.42 | ❌ 完全失败 |
| ② | GC KMeans + ICC PCA + 论文权重 | -0.42 | ❌ PCA系数是主因 |
| ③ | GC KMeans + GC PCA + 论文权重 | 0.586 | ⚠️ 框架开始工作 |
| ④ | **GC KMeans + GC PCA + GC权重** | **0.857** | ✅ 框架有效 |

**建议PPT配图**：4步递进流程图，每步标注r值

**介绍话术**：
> "通过逐步替换组件发现，ICC模型参数不能直接迁移到GC。但论文框架（KMeans→PCA→加权F）在GC上重新训练后，r达到0.857，LOO交叉验证后仍保持0.806，说明框架本身具有跨癌种适用性。"

### 5.3 关键发现：P2是跨癌种稳定因子

| 分量 | r(ΔP vs log10ATP) | 有效？ |
|------|:---:|:---:|
| ΔP1 | -0.04 | ❌ |
| ΔP2 | **0.81** | ✅ 主力 |
| ΔP3 | 0.30 | ⚠️ |
| ΔP4 | 0.15 | ❌ |

**建议PPT配图**：四宫格分量散点图

**介绍话术**：
> "P2单独就能达到r=0.81，P2的高载荷特征主要是Cluster 2（大实心健康态）和Cluster 3（小实心中间态）的形态学参数，这两类在GC中占95%以上。P2可能是跨癌种稳定的预测因子。"

---

## 六、全部实验汇总（PPT 第12-13页）

### 6.1 实验清单

| 编号 | 实验名称 | 脚本 | 关键结果 | 状态 |
|:--:|---------|------|---------|:--:|
| 1 | 论文模型验证（ICC） | `verify_paper_model.py` | 确认论文框架在ICC上有效 | ✅ |
| 2 | 论文r=0.887验证（ICC） | `validate_paper_r0887.py` | 全量训练r=0.938存在 | ✅ |
| 3 | ICC005两天数据验证 | `verify_paper_ICC005.py` | 两天数据F vs ATP | ✅ |
| 4 | **ICC Delta特征增强** | `step7_pca_atp_v2_stratified/` | r=0.931, 但KMO=0.446 | ✅ |
| 5 | ICC KFold交叉验证 | `icc_kfold_cv.py` | LOO CV r=0.783, gap≈0.14 | ✅ |
| 6 | **ICC PCA质量评估** | `analyze_pca_quality.py` | D5+Delta最优, r=0.8735 | ✅ |
| 7 | GC论文框架直接套用 | `verify_paper_GC.py` | r=-0.42, 完全失败 | ✅ |
| 8 | GC框架完整重训 | `verify_paper_GC_step1.py` | r=0.857, LOO CV=0.806 | ✅ |
| 9 | GC ICC PCA系数对比 | `compare_ICC_GC_PCA.py` | 系数完全不同, P2方向相反 | ✅ |
| 10 | GC健康类器官阈值分析 | `analyze_threshold.py` | MIN_HEALTHY=3最优 | ✅ |
| 11 | nnUNet GC KMeans训练 | `train_nnunet_kmeans.py` | 分割精度高但特征偏移 | ✅ |
| 12 | GC032多药物验证 | `verify_paper_GC032.py` | 缺OAC特征, 无法完成 | ❌ |
| 13 | GC 6图生成 | `generate_GC_figures.py` | 对照论文风格生成6图 | ✅ |

### 6.2 核心指标对比

| 维度 | 论文(OAC) | 我们(ICC Delta) | 我们(GC重训) |
|------|:--------:|:--------------:|:----------:|
| 全量训练 r | **0.938** | 0.931 | 0.857 |
| 独立测试 r | **0.887** | ❓ 未完成 | ❓ 未完成 |
| LOO CV r | 未报告 | 0.783 | **0.806** |
| 输入维度 | 44D | 45D→24D | 15D |
| KMO | **0.742** | 0.446→0.853 | — |
| 主要优势 | 已发表+泛化验证 | 信息集中(PC1=73.8%) | 跨癌种框架有效 |
| 主要风险 | — | 共线性+过拟合 | 样本量小(n=36) |

---

## 七、遇到的问题与解决（PPT 第14-15页）

### 问题1：Delta + RelChange 共线性严重

| 问题 | 详情 |
|------|------|
| 表现 | 41对特征 \|r\|>0.9, KMO=0.446, VIF全部inf |
| 根因 | RelChange = Delta/|D3|，与Delta几乎线性相关 |
| 解决 | **删除RelChange组**，精简为D5+Delta（24维） |
| 效果 | KMO 0.446→0.853, d/n 1.09→0.80, r=0.8735 |

### 问题2：过拟合风险

| 问题 | 详情 |
|------|------|
| 表现 | 全量训练r=0.931, LOO CV r=0.783, gap≈0.15 |
| 根因 | 45维特征, 36个样本, d/n=1.25 > 1 |
| 解决 | 降维至24维(d/n=0.80) + 4个Kaiser PCs |
| 待做 | 需在联合用药独立数据上验证泛化能力 |

### 问题3：ICC模型不能直接迁移到GC

| 问题 | 详情 |
|------|------|
| 表现 | ICC KMeans+PCA+权重套用GC, r=-0.42 |
| 根因 | 聚类分布不同、PCA系数方向相反、权重失效 |
| 解决 | 框架全部组件在GC上重新训练 |
| 效果 | r=0.857, LOO CV=0.806 |

### 问题4：GC聚类极不均衡

| 问题 | 详情 |
|------|------|
| 表现 | Cluster 0（大囊状健康态）仅15个对象(0.03%) |
| 影响 | 基于Cluster 0的3个特征不稳定 |
| 解决 | PCA本身对稀疏特征有鲁棒性 |

---

## 八、后续方向（PPT 第16页）

### 🔴 方向1：独立数据泛化验证（最高优先级）

当前所有结果都是**全量训练/交叉验证**，尚无**独立测试集**验证。

```
需要补充：
  1. ICC Delta模型在联合用药数据上的独立测试
  2. GC模型在GC032多药物数据上的独立测试
  3. 对标论文 r=0.887 的泛化能力
```

### 🟡 方向2：简化模型

- 在GC上，P2单独就能达到r=0.81，可能不需要4个分量
- 建议：直接用P2或P2+P3替代F，减少复杂度
- 论文15维静态特征已经很强（r=0.8706），Delta的增益有限

### 🟡 方向3：改进聚类策略

- 当前KMeans在GC上极不均衡（Cluster 0仅0.03%）
- 可尝试：GMM软聚类、分位数聚类、3类聚类

### 🟢 方向4：超参数统一

- 论文固定4个PC，我们按Kaiser动态选择
- 需统一PC选择策略以便公平比较
- 建议：统一使用Kaiser准则（特征值>1）

### 🟢 方向5：Baseline对比

- 直接用11维原始形态学特征median回归ATP
- 与PCA框架对比，确认框架的增益

---

## 九、PPT页面建议（共16页）

| 页码 | 标题 | 配图 | 核心信息 |
|:--:|------|------|------|
| 1 | 论文背景 | 框架流程图 | 44D→4PC, r=0.906/0.938/0.887 |
| 2 | 实验路线总览 | 双分支流程图 | ICC增强 + GC泛化 两条主线 |
| 3 | ICC Delta特征工程 | 特征工程流程图 | 3组新增特征(45D) |
| 4 | ICC Delta核心结果 | `fig2_score_atp.png` | r=0.931, p=3.26e-11 |
| 5 | 强相关Delta特征 | 四宫格散点图 | ΔScattMean r=-0.96 |
| 6 | **问题：共线性暴露** | `feature_correlation_heatmap.png` | 41对 \|r\|>0.9 |
| 7 | **问题：KMO不合格** | KMO/VIF对比表 | KMO=0.446 vs 论文0.742 |
| 8 | **解决：特征子集筛选** | `feature_subset_benchmark.png` | D5+Delta最优, r=0.8735 |
| 9 | GC跨癌种验证设计 | ICC vs GC对比表 | 全部组件需重训 |
| 10 | GC四步递进实验 | 4步流程图 | r从-0.42→0.857 |
| 11 | GC关键发现 | 四宫格分量图 | P2是跨癌种稳定因子 |
| 12 | 全部实验汇总 | 实验清单表 | 13项实验, 12项完成 |
| 13 | 核心指标对比 | 论文vs我们对比表 | 三列对比 |
| 14 | 问题1-2 | 问题→解决→效果表 | 共线性+过拟合 |
| 15 | 问题3-4 | 问题→解决→效果表 | 迁移失败+聚类不均 |
| 16 | 后续方向 | 优先级清单 | 5个方向, 独立验证最优先 |

---

## 十、配图文件清单

| 图文件名 | 所在目录 | 用于PPT第几页 |
|---------|------|:--:|
| `fig1_clustering.png` | `v2_stratified_v.2.0/` | 第12页（附录） |
| `fig2_score_atp.png` | `v2_stratified_v.2.0/` | 第4页 |
| `fig3_dose_response.png` | `v2_stratified_v.2.0/` | 附录 |
| `fig4_score_dist.png` | `v2_stratified_v.2.0/` | 附录 |
| `fig5_radar.png` | `v2_stratified_v.2.0/` | 附录 |
| `fig6_residual.png` | `v2_stratified_v.2.0/` | 附录 |
| `feature_correlation_heatmap.png` | `v2_stratified/` | 第6页 |
| `feature_subset_benchmark.png` | `v2_stratified/` | 第8页 |
| `subset_pca_scree_grid.png` | `v2_stratified/` | 第8页 |
| `kmo_bartlett_report.txt` | `v2_stratified/` | 参考数据 |

---

## 十一、一句话总结

> 论文的KMeans→PCA→加权F框架在ICC上通过Delta特征增强达到r=0.931（接近论文0.938），但PCA质量分析暴露了RelChange组完全冗余（KMO=0.446→0.853），最优子集为D5+Delta(24D, r=0.8735)。框架在GC胃癌上重新训练后有效（r=0.857, LOO CV=0.806），但模型参数不能跨癌种直接迁移。**当前最大缺口是缺少独立测试集验证泛化能力**，需对标论文的r=0.887。