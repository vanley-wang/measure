# 论文框架跨癌种验证 — PPT汇报文档

---

## 一、背景与目标

### 1.1 论文核心内容

论文《基于OAC与多维形态表征的肿瘤类器官无损药物作用评估》提出了一套评估框架：

```
原始形态学数据 → KMeans聚类(4类) → 提取15维特征(X11-X43) → PCA → F = 0.468P1+0.247P2+0.165P3+0.120P4
```

论文声称在ICC（肝内胆管癌）数据上，综合得分F与ATP的相关系数 r = 0.887。

### 1.2 验证目标

**在胃癌（GC）类器官数据上验证该框架是否可跨癌种应用。**

- 数据来源：`FXN_2023_new（GC）`，36个well，Day1(0701) + Day2(0703)
- 包含完整形态学特征 + Scatt（光学衰减系数）
- ATP范围：15,450 ~ 65,380,000（跨4个数量级）

### 1.3 核心区分

| 层面 | ICC（论文） | GC（本次验证） |
|------|------------|---------------|
| 数据 | 肝内胆管癌 | **胃癌** |
| KMeans | ICC训练的 | **GC重新训练** |
| PCA系数 | ICC数据算出 | **GC重新训练** |
| F权重 | 0.468/0.247/0.165/0.120 | **GC重新拟合** |

> ⚠️ 关键认知：ICC和GC是两个完全独立的实验，KMeans、PCA、F权重均需重新训练。**不能直接比较r值高低**。

---

## 二、实验路线图（PPT第2页）

```
实验1: ICC模型直接套用GC
  └─ ICC KMeans + ICC PCA + ICC F权重 → GC数据
     └─ r = -0.42  ❌ 完全失败

实验2: 仅换KMeans
  └─ GC KMeans + ICC PCA + ICC F权重 → GC数据
     └─ r = -0.42  ❌ PCA系数不兼容是主因

实验3: 换KMeans + PCA
  └─ GC KMeans + GC PCA + ICC F权重 → GC数据
     └─ r = 0.586  ⚠️ 框架开始工作

实验4: 全部重训（框架完整应用）
  └─ GC KMeans + GC PCA + GC F权重 → GC数据
     └─ r = 0.857  ✅ 框架有效
     └─ LOO CV: r = 0.806  ✅ 内部泛化良好
```

**建议PPT配图**：用流程图/箭头图展示4个实验的递进关系，每个实验标注r值。

---

## 三、核心结果（PPT第3-4页）

### 3.1 GC框架完整应用结果

| 指标 | 值 |
|------|-----|
| 全量训练 r(F vs log10ATP) | **0.857** |
| LOO交叉验证 r | **0.806** |
| Spearman ρ | 0.784 |
| p-value | 2.5×10⁻¹¹ |
| 前4个PC累计方差 | 83.0% |

**建议PPT配图**：`fig2_score_atp.png` — 核心散点图（F vs log10ATP，按ATP低/中/高着色）

**介绍话术**：
> "在GC数据上，按照论文框架重新训练KMeans、PCA和F权重后，综合得分F与log10(ATP)的相关系数达到0.857，LOO交叉验证后仍保持0.806。说明该框架在胃癌类器官上同样有效。"

### 3.2 GC拟合的F权重

| 分量 | 论文权重(ICC) | GC拟合权重 | 说明 |
|------|:-----------:|:--------:|------|
| w1(ΔP1) | 0.468 | **-0.065** | GC上P1几乎无贡献 |
| w2(ΔP2) | 0.247 | **0.471** | GC上P2是绝对主力 |
| w3(ΔP3) | 0.165 | 0.135 | 贡献接近 |
| w4(ΔP4) | 0.120 | 0.121 | 几乎相同 |

**建议PPT配图**：`fig2_score_atp.png` 中的权重柱状图 + `fig6_components.png`（四宫格分量散点图）

**介绍话术**：
> "GC拟合的权重与论文完全不同。P2在GC上贡献了47%的权重，而P1几乎失效。四宫格散点图可以直观看到：只有ΔP2与ATP有强相关性(r=0.81)，其他三个分量几乎没有预测能力。"

---

## 四、关键发现：P2是跨癌种稳定的预测因子（PPT第5页）

### 4.1 各分量独立预测能力

| 分量 | r(ΔP vs log10ATP) | 有效？ |
|------|:---:|:---:|
| ΔP1 | -0.04 | ❌ |
| ΔP2 | **0.81** | ✅ |
| ΔP3 | 0.30 | ⚠️ |
| ΔP4 | 0.15 | ❌ |

**建议PPT配图**：`fig6_components.png`

**介绍话术**：
> "P2单独就能达到r=0.81，比论文的4分量固定权重组合(r=0.61)高出0.2。P2的高载荷特征主要是Cluster 2（大实心健康态）和Cluster 3（小实心中间态）的形态学参数，这两类在GC中占95%以上。"

### 4.2 相关性矩阵

**建议PPT配图**：`fig5_correlation.png`

**介绍话术**：
> "相关性矩阵显示，P2与ATP的相关性(r=0.78)远超其他分量。P1与ATP呈负相关(r=-0.25)，解释了为什么论文给P1最高权重(0.468)在GC上完全失效。"

---

## 五、GC聚类特征（PPT第6页）

### 5.1 聚类分布（GC KMeans, 4类）

| Cluster | 表型 | Day1 | Day2 | 占比 |
|:---:|------|-----:|-----:|:---:|
| 0 | 大囊状(H) | 1 | 14 | 0.03% |
| 1 | 大实心(H) | 203 | 182 | 0.7% |
| 2 | 小实心(I) | 8,605 | 8,136 | 31.8% |
| 3 | 极小(D) | 21,769 | 13,672 | 67.4% |

**建议PPT配图**：`fig1_clustering.png` — PCA散点 + 热力图

**介绍话术**：
> "GC的聚类分布与ICC截然不同。Cluster 0（大囊状健康态）仅15个对象(0.03%)，Cluster 3（极小致密态）占67.4%。这反映了胃癌类器官本身的生物学特征——极少形成囊状结构。虽然聚类极不均衡，但热力图显示4类在形态学特征上仍有清晰区分。"

---

## 六、遇到的问题与解决（PPT第7页）

### 问题1：ICC模型直接套用GC完全失败

| 实验 | 方法 | 结果 |
|:---:|------|:---:|
| ① | ICC KMeans + ICC PCA + 论文权重 | r = -0.42 |
| ② | GC KMeans + ICC PCA + 论文权重 | r = -0.42 |

**原因**：ICC的PCA系数依赖ICC数据的协方差结构。GC数据上ICC-P2与GC-P2的相关系数仅-0.82（方向完全相反）。

**建议PPT配图**：`compare_ICC_GC_PCA.py` 输出的系数对比表

**解决**：所有组件（KMeans、PCA、F权重）必须在目标数据上重新训练。

### 问题2：GC聚类极不均衡

Cluster 0（大囊状健康态）仅15个对象，导致X11-X13特征几乎全是噪声。

**影响**：基于Cluster 0的15维特征中，前3个特征不稳定。

**解决**：PCA本身能处理稀疏特征，前4个PC仍累计83%方差，框架对此有一定鲁棒性。

### 问题3：GC缺乏药物浓度信息

GC数据只有ATP值，没有药物浓度分组（ICC有0/20/40/80 μM）。

**影响**：无法生成剂量-反应曲线，只能按ATP大小分组展示。

**建议PPT配图**：`fig3_composition.png` + `fig4_ranking.png`

---

## 七、已完成的辅助分析（PPT附录）

| 分析项 | 脚本 | 结论 |
|--------|------|------|
| 健康类器官阈值敏感性分析 | `analyze_threshold.py` | MIN_HEALTHY_SAMPLES=3最优 |
| nnUNet分割数据KMeans训练 | `train_nnunet_kmeans.py` | 分割精度高但特征分布偏移 |
| ICC KFold交叉验证 | `icc_kfold_cv.py` | ICC模型过拟合gap≈0.14 |
| ICC PCA vs GC PCA系数对比 | `compare_ICC_GC_PCA.py` | 系数完全不同，P2方向相反 |
| GC032多药物验证 | `verify_paper_GC032.py` | 缺OAC特征，无法完成 |
| GC 6图生成 | `generate_GC_figures.py` | 对照论文风格生成6张图 |

---

## 八、后续方向

### 方向1：简化模型（投入产出比最高）

P2单独就能达到r=0.81，可能不需要4个分量。建议：
- 直接用P2替代F，减少复杂度
- 或只用P2+P3两个分量（r=0.846，接近全量）

### 方向2：Baseline对比

直接用原始形态学特征（11维median）回归ATP，与论文框架对比：
- 如果简单方法r更高 → 框架没有增益
- 如果简单方法r更低 → 框架确实有价值

### 方向3：改进聚类策略

当前KMeans在GC上极不均衡，可尝试：
- GMM（高斯混合模型）软聚类
- 分位数聚类替代KMeans距离聚类
- 3类聚类（合并Cluster 0+1）

### 方向4：跨癌种特征对齐

如果要做真正的"模型迁移"（而非框架迁移），需要：
- 分位数归一化：将GC特征映射到ICC的分位数空间
- 域适应（Domain Adaptation）

---

## 九、PPT页面建议

| 页码 | 标题 | 配图 | 核心信息 |
|:--:|------|------|------|
| 1 | 背景与目标 | 论文框架流程图 | 验证框架在GC上是否有效 |
| 2 | 实验路线图 | 4步递进流程图 | 逐步替换→r从-0.42到0.857 |
| 3 | 核心结果 | fig2_score_atp.png | r=0.857, LOO CV=0.806 |
| 4 | 分量分析 | fig6_components.png | P2独大，P1失效 |
| 5 | 权重对比 | 论文vs GC权重表 | 论文权重不适用于GC |
| 6 | GC聚类特征 | fig1_clustering.png | 聚类极不均衡但有效 |
| 7 | 问题与解决 | 对比表 | 三大问题及解决方案 |
| — | 附录 | fig3/4/5 | 补充分析 |

---

## 十、一句话总结

> 论文提出的 KMeans→15特征→PCA→加权F 框架在胃癌类器官数据上重新训练后有效（r=0.857, LOO CV=0.806），但论文的具体模型参数（KMeans/PCA/权重）不能跨癌种直接迁移，必须全部重新训练。