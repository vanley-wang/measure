# 论文原文完整流程：四类类器官（红/黄/绿/蓝Cluster1~4）计算、判定、原文写法
## 一、完整计算分步（论文4.2节完整原文逻辑）
### 步骤1：提取单个类器官全部多维特征（输入数据）
经ParaSAM2CNN三维分割后，对**23439个有效iCCA类器官**逐样本提取**形态+光学两类特征**：
1. **形态参数**：实体体积、填充体积、空腔体积、表面积、长轴、短轴、囊壁厚度、球度、粗糙度
2. **光学OAC参数**：OAC均值、OAC标准差
合计10维原始特征，构成每个类器官的特征向量。

### 步骤2：Z-score标准化消除量纲干扰（论文4.2.1）
体积数值是`10⁻³ mm³`量级，OAC仅`1~3 mm⁻¹`，数值跨度差距极大；直接算欧氏距离会让体积完全主导聚类、OAC信息被淹没。
采用**Z-score标准化**：
$$z=\frac{x-\mu}{\sigma}$$
把所有特征映射至**均值0、标准差1**，保证形态、光学特征在距离计算中等权重。
配套佐证：论文绘制特征相关性热图，证实体积类特征高度自相关，OAC与形态弱相关，必须标准化后聚类。

### 步骤3：手肘法确定最优聚类数k=4（论文4.2.2，图4-2）
遍历k=2~8，计算每组聚类**簇内误差平方和SSE**，绘制SSE折线图：
- k=1~4时SSE快速下降；
- k≥4后下降幅度显著放缓，出现明显“肘部拐点”；
综合生物学可解释性（健康囊、大实心、小实心、受损高致密四类生理状态），**确定最优聚类数量k=4**。

### 步骤4：K-means++无监督聚类分配类别标签（核心计算）
1. **初始化优化（K-means++）**
    不随机选初始中心：随机选第1个样本，剩余中心优先选取距离现有中心更远的样本，规避普通K-means局部最优缺陷；设置最大迭代300次，重复10次不同随机种子取最优收敛结果。
2. **迭代分配规则**
    ① 计算每个标准化类器官向量到4个聚类质心**欧氏距离**；
    ② 将该类器官划分到**距离最近**的质心所属簇（Cluster1/2/3/4）；
    ③ 重新计算每簇所有样本特征均值，更新4个簇质心；
    ④ 循环迭代至质心无明显变化，收敛停止；
3. **输出结果**：每个类器官自动获得唯一聚类标签`1/2/3/4`，对应四种生理表型。

### 步骤5：根据每簇质心特征定义四类名称+配色（论文4.2.2箱线图4-3、表4-1）
聚类完成后，统计**每簇所有特征均值**，根据体积、空腔、OAC三大核心指标区分四类：
| 聚类编号 | 配色 | 官方命名 | 核心特征判定标准（质心均值） |
| ---- | ---- | ---- | ---- |
| Cluster1 | 红色 | 大囊状健康类器官 | 填充体积最大；空腔体积显著非零；OAC均值最低 |
| Cluster2 | 黄色 | 大实心健康类器官 | 体积次大；无空腔；低OAC，细胞排列致密但无凋亡损伤 |
| Cluster3 | 绿色 | 小实心休眠/幼类器官 | 体积微小、无空腔；OAC中等偏低，发育早期/自然停滞 |
| Cluster4 | 蓝色 | 极小高致密受损类器官 | 体积最小；无空腔；OAC均值显著远高于其他三类（凋亡核固缩、细胞器重构） |

### 步骤6：可视化映射（图4-4、4-7）
三维OCT渲染图中，给四类簇分别绑定红/黄/绿/蓝伪彩色，一眼区分群体内不同生理状态的类器官分布；统计每孔四类数量占比、各簇平均OAC/体积，用于后续药效建模。

## 二、论文原文标准写法（分段落摘抄式模板，可直接复用）
### 1. 特征预处理段落（4.2.1原文风格）
> 对经ParaSAM2CNN自动分割得到的全部类器官单体提取多维表征参数，涵盖填充体积、空腔体积、表面积、球度、粗糙度等形态指标与OAC均值、OAC标准差光学指标。由于各特征量纲与数值量级差异巨大，直接聚类会造成大尺度体积参数主导距离计算，削弱光学衰减系数的区分作用，因此采用Z-score标准化对全部特征无量纲化处理，将各维度特征映射至均值为0、标准差为1的标准正态分布。通过Pearson相关系数矩阵分析形态参数间多重共线性，为后续聚类与降维提供数据基础。

### 2. k值确定（手肘法原文写法）
> 采用手肘法遍历聚类数目k=2至8，计算不同分簇下簇内误差平方SSE并绘制衰减曲线。结果显示k=4时曲线出现显著拐点，继续增加聚类数量对SSE的改善幅度有限；结合肿瘤类器官生理发育与药物损伤的四种典型表型，确定最优聚类数目为4。

### 3. K-means++聚类算法描述（论文标准表述）
> 采用K-means++无监督聚类算法对标准化后的类器官高维特征集进行分簇。算法通过距离加权概率机制优化初始聚类中心选取，降低随机初始化带来的局部最优风险；设置最大迭代次数300，重复10次不同初始化种子选取收敛最优模型。迭代过程中计算每个类器官特征向量至4个簇质心的欧氏距离，将样本分配至距离最小的簇，并迭代更新各簇质心直至收敛。全部23439个有效类器官单体完成自动分簇，得到4类具有明确生物学意义的类器官亚群。

### 4. 四类表型定义原文段落
> 聚类完成后统计四类簇的填充体积、空腔体积、平均OAC核心特征，划分四类典型类器官表型：Cluster1（红色）为大囊状活跃类器官，具备大体积与完整空腔结构，光学衰减水平最低；Cluster2（黄色）为大实心增殖类器官，无空腔、体积次之，OAC维持低水平；Cluster3（绿色）为小型实心未成熟类器官，尺寸微小、衰减中等；Cluster4（蓝色）为药物诱导高致密受损类器官，体积最小、无空腔，OAC均值显著升高，对应细胞凋亡、核固缩的损伤状态。在三维OCT可视化渲染中为四类簇分别赋予红、黄、绿、蓝伪彩，直观展示药物干预前后类器官群体表型分布演变。

## 三、关键补充：怎么“判断单个类属于哪一类”实操逻辑
1. 算法层面：程序内部存储**4个收敛后的簇质心向量**；
2. 新/旧任意类器官，先做Z-score标准化；
3. 分别计算该样本向量与4个质心的欧氏距离；
4. 距离最小的那个簇编号，即为该类器官所属类别，对应固定配色；
5. 举例：某类器官标准化向量离Cluster4质心最近，自动标记蓝色，判定为高致密受损型。

## 四、聚类结果用途（论文里为什么要分四类）
1. 可视化直观展示药物作用表型转变（对照组Cluster1增多，高药浓度Cluster1消失、Cluster4大量增加）；
2. 提取每孔四类数量、各簇平均OAC/形态指标，构建44维群体统计特征，用于后续PCA主成分与综合生长得分模型；
3. 解决“同一孔内类器官异质性”问题，不用整体均值掩盖不同损伤程度的个体差异，提升药效评估精细度。